name: CI

on:
  pull_request:
  push:
    branches: [main]

jobs:
  backend:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-dotnet@v4
        with:
          dotnet-version: '8.0.x'
      - name: Restore
        run: dotnet restore artifacts/api-server-dotnet/JobRadar.Api.csproj
      - name: Build
        run: dotnet build artifacts/api-server-dotnet/JobRadar.Api.csproj --no-restore --configuration Release

  frontend:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: npm
          cache-dependency-path: artifacts/job-radar/package-lock.json
      - name: Install
        working-directory: artifacts/job-radar
        run: npm ci
      - name: Typecheck
        working-directory: artifacts/job-radar
        run: npm run typecheck
      - name: Build
        working-directory: artifacts/job-radar
        run: npm run build
